import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CRC_Associates_LLP',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
